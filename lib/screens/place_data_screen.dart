import 'package:flutter/material.dart';

class PlacesDataScreen extends StatelessWidget {
  const PlacesDataScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center();
  }
}
